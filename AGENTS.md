# PacketCinema 開発ガイド

## 目的

通信の順序と理由を、移動するパケットと日本語の解説で学ぶWeb教材。
通信のアニメーションは論理メッセージの表現であり、実測時間や実パケット取得ではない。

## 作業の入口

- 教材追加・シナリオ拡張：`.agents/skills/packetcinema-add-protocol/SKILL.md`。
- 教材の技術レビュー・画面レビュー：`.agents/skills/packetcinema-review/SKILL.md`。
- 設計・依存関係の変更：[設計文書](docs/architecture.md)と`tests/contracts/dependencies.test.ts`。
- 描画変更：`src/visualization/`。再生・操作変更：`src/player/`。
- 教材登録・URL変更：`src/app/catalog.ts`と`src/app/routing/`。
- push・Pages公開：[公開手順](docs/publishing.md)。

作業に関係する入口と変更対象から読む。過去チャットや全教材を毎回読み直さない。
仕様の詳細は既存文書・型・テストを参照し、このファイルに複製しない。

## ブランチとPR

- 変更作業は原則1件につき1本の短期ブランチ（`codex/<作業内容>`）で行い、mainへ直接コミット・pushしない。明示されたブランチ指定は優先する。
- 着手時にブランチと既存差分を確認する。同じ作業のブランチは継続し、新規作業は最新のmainから作る。未コミット変更や未pushコミットを破棄・混在させない。
- mainへの取り込みはPR経由とし、差分と検証結果を確認してSquash mergeする。作業ブランチの完了後は次の作業に流用しない。
- 詳細は[ブランチ・PR運用](docs/git-workflow.md)。commit・push・PR投稿・マージ・公開は会話で依頼された範囲で行う。この規約自体は外部送信や公開の許可を与えない。

## 維持する基準

- 共通プレーヤー・描画層にプロトコル名や教材のステップIDによる分岐を追加しない。
- 別プロトコルの部品は`index.ts`の公開窓口から再利用する。
- 技術的な説明と出典の対応を確認し、教材の前提・省略・実装依存の違いを明示する。
- 状態を持つ場面は途中URLからも復元できるようにする。
- 既存のURL・再生操作を維持する。教材本文の意図した改訂で基準データを更新するときは差分を確認する。

## 検証

- 教材追加は追加スキルと`docs/adding-protocol.md`に従う。
- コード変更は関連テストと`npm run build`。画面・再生変更は関連E2Eと実際の表示も確認する。
- 文書だけの変更はリンク・内容の整合性を確認し、アプリの全テストを機械的に実行しない。
- 同じ差分の検証成功後は、追加変更や未解決の懸念がなければ繰り返さない。
- 完了報告には実施した検証と未検証範囲を簡潔に記載する。

## 作業範囲

依頼された実装・レビュー・公開の範囲を維持する。レビューだけの依頼では修正しない。
サブエージェントは常時起動しない。必要な独立レビューの分担は依頼時に検討する。
